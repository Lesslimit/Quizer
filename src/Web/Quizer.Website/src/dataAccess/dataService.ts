import {HttpClient} from 'aurelia-fetch-client';

export class DataService {
    students = {
        getAll() {
            let client = new HttpClient();

            client.fetch
        }
    }
}
